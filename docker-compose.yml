services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sufis_market
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
      - 1.1.1.1
    environment:
      NODE_ENV: production
      NODE_OPTIONS: "--max-old-space-size=4096"
      NEXT_PUBLIC_BASE_API: https://api.sufismarket.com/api
      NEXT_PUBLIC_BASE_URL: https://api.sufismarket.com
      NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: AIzaSyBFHDroNvBvr33SCfRaWDjZZVJKUafEDb8
    ports:
      - "3000:3000"